---
# Source: nifi-operator/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: nifi-operator-serviceaccount
  labels:
    helm.sh/chart: nifi-operator-0.5.0
    app.kubernetes.io/name: nifi-operator
    app.kubernetes.io/instance: nifi-operator
    app.kubernetes.io/version: "0.5.0"
    app.kubernetes.io/managed-by: Helm
---
# Source: nifi-operator/templates/serviceaccount.yaml
apiVersion: rbac.authorization.k8s.io/v1
# This cluster role binding allows anyone in the "manager" group to read secrets in any namespace.
kind: ClusterRoleBinding
metadata:
  name: nifi-operator-clusterrolebinding
  labels:
    helm.sh/chart: nifi-operator-0.5.0
    app.kubernetes.io/name: nifi-operator
    app.kubernetes.io/instance: nifi-operator
    app.kubernetes.io/version: "0.5.0"
    app.kubernetes.io/managed-by: Helm
subjects:
  - kind: ServiceAccount
    name: nifi-operator-serviceaccount
    namespace: default
roleRef:
  kind: ClusterRole
  name: nifi-operator-clusterrole
  apiGroup: rbac.authorization.k8s.io
